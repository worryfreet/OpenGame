import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-回归测试';
export const playletTitle = '回归测试';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('回归测试PlayletScene');
  }
}
