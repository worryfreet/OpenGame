import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-论证表达';
export const playletTitle = '论证表达';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('论证表达PlayletScene');
  }
}
