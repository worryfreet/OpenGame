import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-多角色决策';
export const playletTitle = '多角色决策';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('多角色决策PlayletScene');
  }
}
