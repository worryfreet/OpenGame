import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-接落物';
export const playletTitle = '接落物';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('接落物PlayletScene');
  }
}
