import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-调参-plus-对比';
export const playletTitle = '调参 + 对比';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('调参Plus对比PlayletScene');
  }
}
