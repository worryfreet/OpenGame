import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-资源分配';
export const playletTitle = '资源分配';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('资源分配PlayletScene');
  }
}
