import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-控制变量实验';
export const playletTitle = '控制变量实验';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('控制变量实验PlayletScene');
  }
}
