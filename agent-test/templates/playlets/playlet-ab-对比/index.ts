import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-ab-对比';
export const playletTitle = 'A/B 对比';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('Ab对比PlayletScene');
  }
}
