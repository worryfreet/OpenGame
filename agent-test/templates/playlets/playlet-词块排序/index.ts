import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-词块排序';
export const playletTitle = '词块排序';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('词块排序PlayletScene');
  }
}
