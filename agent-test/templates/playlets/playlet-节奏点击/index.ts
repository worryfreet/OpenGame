import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-节奏点击';
export const playletTitle = '节奏点击';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('节奏点击PlayletScene');
  }
}
