import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-点击射击';
export const playletTitle = '点击射击';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('点击射击PlayletScene');
  }
}
