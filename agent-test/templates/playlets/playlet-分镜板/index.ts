import { GenericPlayletScene } from '../shared';

export const playletId = 'playlet-分镜板';
export const playletTitle = '分镜板';

export class PlayletScene extends GenericPlayletScene {
  constructor() {
    super('分镜板PlayletScene');
  }
}
